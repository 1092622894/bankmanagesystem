<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gdut.bankmanagesystem.mapper.DepartmentMapper">
    <resultMap id="DepartmentViewResult" type="DepartmentView"/>

    <select id="queryDepartmentInfoById" resultMap="DepartmentViewResult" parameterType="Long">
        select d.id, d.name, b.name, b.city, e.name where department d left join
        on d.b_id = b.id left join e on e.id = d.d_id where d.id = {id}
    </select>

    <select id="queryAllDepartmentInfo" resultMap="DepartmentViewResult">
       select d.id, d.name, b.name, b.city, e.name where department d left join
        on d.b_id = b.id left join e on e.id = d.d_id
    </select>

</mapper>
